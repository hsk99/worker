<?php

namespace App\Crontab\{_process_};

class Test
{
    public static $run = false;
    
    public static $rule = '1 * * * * *';
    
    public static function init(object $crontab, object $worker)
    {
        var_dump($worker->id);
    }
}
