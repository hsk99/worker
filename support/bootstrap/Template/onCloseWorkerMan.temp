<?php

namespace App\Callback\{_process_};

use support\bootstrap\Log;

class onClose
{
    public static function init($connection)
    {
        log::info('close', ['connection_id' => $connection->id]);
    }
}
